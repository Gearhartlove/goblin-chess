defmodule GoblinChessServer.Database do
  def store(event_name, data) do 
    # store the data in a postgres database that is also connected to the server
    # TODO: set up an ecto schema in this project that accurately reflects this setup
  end
end
